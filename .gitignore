# Dependencies
node_modules
.pnp
.pnp.js

# Build
.next
out
build
dist
.turbo

# Testing
test-output
coverage
playwright-report
test-results
.playwright-mcp
**/.stryker-tmp

# Misc
.DS_Store
*.pem

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# SQLite (legacy - can be removed if no longer used)
**/*.db
**/*.db-journal

# Uploads (any data/uploads directory in the repo)
**/data/uploads/*
!**/data/uploads/.gitkeep

# Database Backups (large binary files, not checked into version control)
backups/
*.sql.gz
*.sql.bz2
*.sql.xz
!backups/.gitkeep

# Lockfiles (use pnpm)
bun.lock
package-lock.json
yarn.lock

# IDE
.idea

# VS Code (allow shared settings, ignore personal/local files)
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json

# Ladle cache
apps/web/.ladle/.cache

# Claude files
!.claude
!CLAUDE.md
.claude/settings.local.json
