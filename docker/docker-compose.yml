name: vamsa

services:
  # PostgreSQL Database
  db:
    image: postgres:18-alpine
    container_name: vamsa-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-vamsa}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required - see .env.example}
      POSTGRES_DB: ${DB_NAME:-vamsa}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=en_US.UTF-8"
    command:
      - "postgres"
      - "-c"
      - "max_connections=100"
      - "-c"
      - "shared_buffers=256MB"
      - "-c"
      - "effective_cache_size=768MB"
      - "-c"
      - "maintenance_work_mem=64MB"
      - "-c"
      - "checkpoint_completion_target=0.9"
      - "-c"
      - "wal_buffers=16MB"
      - "-c"
      - "default_statistics_target=100"
      - "-c"
      - "random_page_cost=1.1"
      - "-c"
      - "effective_io_concurrency=200"
      - "-c"
      - "work_mem=4MB"
      - "-c"
      - "huge_pages=off"
      - "-c"
      - "min_wal_size=1GB"
      - "-c"
      - "max_wal_size=4GB"
    expose:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-vamsa}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Vamsa Application (Bun + Hono + TanStack Start)
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        SKIP_SSL_VERIFY: ${SKIP_SSL_VERIFY:-}
    container_name: vamsa-app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 3000
      HOST: 0.0.0.0
      DATABASE_URL: "postgresql://${DB_USER:-vamsa}:${DB_PASSWORD:?DB_PASSWORD is required}@db:5432/${DB_NAME:-vamsa}"
      DB_SSL_MODE: disable
      BETTER_AUTH_SECRET: "${BETTER_AUTH_SECRET:?BETTER_AUTH_SECRET is required}"
      TRUSTED_PROXIES: "docker"
    ports:
      - "${APP_PORT:-80}:3000"
    volumes:
      - uploads_data:/app/data/uploads
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget --no-verbose --tries=1 --spider http://localhost:3000/health || exit 1",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Vamsa AI Service (opt-in, requires --profile ai)
  ai:
    build:
      context: ..
      dockerfile: apps/ai/Dockerfile
    container_name: vamsa-ai
    restart: unless-stopped
    profiles:
      - ai
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: production
      AI_PORT: 3100
      AI_HOST: 0.0.0.0
      TOOL_MODE: direct
      DATABASE_URL: "postgresql://${DB_USER:-vamsa}:${DB_PASSWORD:?DB_PASSWORD is required}@db:5432/${DB_NAME:-vamsa}"
      LLM_PROVIDER: "${LLM_PROVIDER:-ollama}"
      LLM_URL: "${LLM_URL:-http://host.docker.internal:11434/v1}"
      LLM_MODEL: "${LLM_MODEL:-qwen2.5:1.5b}"
      LLM_API_KEY: "${LLM_API_KEY:-}"
    expose:
      - "3100"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget --no-verbose --tries=1 --spider http://localhost:3100/healthz || exit 1",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

volumes:
  postgres_data:
    name: vamsa-postgres-data
  uploads_data:
    name: vamsa-uploads-data

networks:
  default:
    name: vamsa-network
